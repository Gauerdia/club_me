import 'package:flutter/material.dart';

class SharingAlertDialog extends StatelessWidget {
  const SharingAlertDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
